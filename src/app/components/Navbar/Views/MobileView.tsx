const MobileView = () => {
    const styles = {
        borders: "border-b border-black"
    };
    return (
        <nav className={`${styles.borders}`}>
            This is the Mobile Menu
        </nav>
    )
};

export { MobileView };